# node_modules
node_modules

# api keys
apiKey.js

# env files
.env*

# dist folder for built files
dist

# Yarn PnP - Zero-Install 전략
# .yarn/cache를 커밋하여 yarn install 없이 바로 사용 가능
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Yarn - 임시 파일만 제외
.yarn/install-state.gz
.yarn/unplugged
.yarn/build-state.yml
*storybook.log
storybook-static
